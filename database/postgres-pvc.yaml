apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
spec:
  accessModes:
    - ReadWriteOnce # Том может быть примонтирован только к одному поду
  resources:
    requests:
      storage: 1Gi
